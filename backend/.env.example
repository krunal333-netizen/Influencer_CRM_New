# Database connection
DATABASE_URL="postgresql://user:password@localhost:5432/influencer_crm"

# HTTP server configuration
PORT=3000
FRONTEND_URL="http://localhost:5173"
COOKIE_DOMAIN=localhost

# JWT secrets & expirations
JWT_ACCESS_SECRET="replace_me_with_a_secure_value"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="replace_me_with_another_secure_value"
JWT_REFRESH_EXPIRES_IN="7d"

# Apify integration (optional - leave empty for dry-run mode only)
APIFY_API_KEY=""
