{
  "name": "influencer-crm",
  "version": "0.0.1",
  "description": "Influencer CRM backend",
  "private": true,
  "type": "module",
  "scripts": {
    "backend": "cd backend && pnpm",
    "prisma": "cd backend && pnpm prisma",
    "migrate": "cd backend && pnpm prisma migrate dev"
  },
  "workspaces": ["backend"],
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
